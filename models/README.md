# models/

Neural model artifacts (TFLite, ONNX, raw weights). Empty until Phase 3 (hybrid v1 with neural RES post-filter).

Large binary weights are excluded by `.gitignore`; store the canonical artifacts on shared storage and document the pointer here when populated.

Tracked via git: only documentation, conversion scripts, and small reference fixtures.
